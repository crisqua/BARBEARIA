# Copie para .env. Sem roteamento por subdomínio ainda (Sprint 3 preparou o
# backend, mas o DNS/hosting fica pra depois) — cada build do app aponta pra
# UM tenant via slug, o que já é fiel ao modelo final (slug.barberaria.app).
VITE_API_URL=http://localhost:3000
VITE_TENANT_SLUG=minha-barbearia
